import React from 'react';
import './login.css'

const Login = (props)=>(
    <div className="container">
        <p className="App-header">
     {props.header}
        </p>
    </div>
);


export default Login;
